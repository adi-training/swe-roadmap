Yes. Use this as the Day 2 program-practice queue—do only the first three today.

| Priority | Program | Skills | Expected outcome |
|---:|---|---|---|
| 1 | Vector printer | `vector`, index loop, `const Type&` | `{4, 7, 1}` prints `[4, 7, 1]` |
| 2 | Sum and average | Traversal, accumulator, `double` cast | `{4, 7, 1, 9}` → sum `21`, average `5.25` |
| 3 | Scale vector in place | `Type&`, reference loop, mutation | `{2, 4, 6}`, factor `3` → `{6, 12, 18}` |
| 4 | Minimum and maximum | One-pass min/max, negative-value edge cases | `{-8, 4, 15}` → min `-8`, max `15` |
| 5 | Append value | `push_back`, pass-by-reference | `{1, 2}` + `3` → `{1, 2, 3}` |
| 6 | Line counter | `ifstream`, `getline`, error handling | A five-line file → `Total lines: 5` |
| 7 | File preview | Vector of strings, file processing | Print only the first three file lines |
| 8 | Word counter | Stream extraction with `inputFile >> word` | `hello world\nC++ is fun` → `5` words |
| 9 | Move zeroes | Two pointers, in-place updates | `{0, 1, 0, 3, 12}` → `{1, 3, 12, 0, 0}` |

For today, your minimum effective practice is:

- Program 1: Vector printer
- Program 2: Sum and average
- Program 3: Scale vector in place
- Project task: Log-file line count and preview

Programs 4–9 are optional reinforcement; carry them into the weekend if needed.